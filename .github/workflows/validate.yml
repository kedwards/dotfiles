---
name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  shell:
    name: Shell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: ludeeus/action-shellcheck@master

      - name: Install shfmt
        run: |
          version="$(gh release view --repo mvdan/sh --json tagName --jq .tagName)"
          curl -fsSL "https://github.com/mvdan/sh/releases/download/${version}/shfmt_${version}_linux_amd64" -o /usr/local/bin/shfmt
          chmod +x /usr/local/bin/shfmt

      - name: shfmt
        run: shfmt -d .

  chezmoi:
    name: Chezmoi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install chezmoi
        run: sh -c "$(curl -fsLS get.chezmoi.io)" -- -b /usr/local/bin

      - name: Dry run
        run: chezmoi init --apply --dry-run --source .

  lua:
    name: Lua
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install stylua
        uses: JohnnyMorganz/stylua-action@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: latest
          args: --check .
      - name: Install selene
        run: |
          version="$(gh release view --repo Kampfkarren/selene --json tagName --jq .tagName)"
          curl -fsSL "https://github.com/Kampfkarren/selene/releases/download/${version}/selene-${version}-linux.zip" -o /tmp/selene.zip
          unzip -q /tmp/selene.zip -d /tmp/selene
          sudo install -m 755 /tmp/selene/selene /usr/local/bin/selene

      - name: selene
        working-directory: home/dot_config/nvim
        run: selene .

  toml:
    name: TOML
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install taplo
        run: |
          curl -fsSL https://github.com/tamasfe/taplo/releases/latest/download/taplo-linux-x86_64.gz | gunzip > /usr/local/bin/taplo
          chmod +x /usr/local/bin/taplo

      - name: taplo fmt check
        run: taplo fmt --check

  docker:
    name: Docker Bootstrap
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Build
        run: docker build -t dotfiles-test .

      - name: Run
        run: docker run dotfiles-test
